{% extends "_template.html.twig" %}

{% block title %}
    {{ category.name }}
{% endblock %}

{% block content %}
    <div class="section">
        <h1 class="title">{{ category.name }}: {{ category.products|length }}</h1>

        {% include 'includes/product_list.html.twig' with {products: category.products} %}
    </div>
{% endblock %}
